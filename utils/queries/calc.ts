export const queryCalcResult = `query CalcResult ($locale: I18NLocaleCode, $carclass: String!, $series: String!) { calcs(filters: { car_class_calc: { slug: { eq: $carclass } }, and: { series: { slug: { eq: $series } } } }, locale: $locale) { __typename data { id __typename attributes { series { data { attributes { title slug } } } result { __typename id place item { id thickness quanity product { data { attributes { title slug   series { data { attributes { title } } } } } } } } } } } }`
